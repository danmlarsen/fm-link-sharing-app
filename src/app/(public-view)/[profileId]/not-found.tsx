import { Card, CardContent } from "@/components/ui/card";

export default function ProfileNotFoundPage() {
  return (
    <Card>
      <CardContent>Profile could not be found.</CardContent>
    </Card>
  );
}
